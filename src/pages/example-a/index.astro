---
import { getCollection } from 'astro:content';
import CategoryPage from '../../components/CategoryPage.astro';
const entries = await getCollection('content');
---
<CategoryPage domain="example-a" entries={entries} />
